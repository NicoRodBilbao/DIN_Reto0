package din_reto0.interfaces;

import java.util.ResourceBundle;
    /**
     * This interface had a method to get the greeting through different means
     * 
     * @author Nico-Nerea
     */
public interface Model {
    String getGreeting();
}
