package din_reto0.interfaces;
    /**
     * This interface has a method that shows the greeting by any means
     * 
     * @author Nico-Nerea
     */
public interface View {
    void showGreeting(String greeting);
}
